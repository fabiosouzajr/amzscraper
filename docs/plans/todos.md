## when performing 
modify the frontend component ui "price-drop-card" in dasboard.tsx, making "drop-header" into a 2 column layout. the left column containing "price-info" and the right containing "drop-header-content". 
-reduce the "drop-amount" size making it more proportional. 
improve the "product-categories" display.
make the recharts graph datapoints(dates) standout.

## 1 Check and scrape for additional parameters:
let's use the item page "https://www.amazon.com.br/Air-cooler-processador-silencioso-eficiente/dp/B00IT14NVO" as an example; to check the selectors for following parameters:
 -- out of stock or unavailable indication
 -- remaining qty available (Somente 4 em estoque.)
 -- discounted item (ex. 60%)
what i want you to do:
in order to write up a md file that details a step by step plan
 these parameters should be incorporated and displayed into the Price Change Dashboard and 

 ## implement a fallback to get product images as some items do not have images